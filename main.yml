---
# tasks file for vsftpd

- name: "Install vsftpd"
  apt:
    name: vsftpd
    state: present

- name: "Launching vsftpd"
  service:
        name: vsftpd
        state: started
        enabled: yes

